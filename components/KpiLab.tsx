// Superseded by the /kpi-lab route hierarchy (KpiLabLanding → KpiIndex → KpiDetail).
// Kept as a thin re-export so any stray import still resolves.
export { default } from "@/components/kpi/KpiLabLanding";
